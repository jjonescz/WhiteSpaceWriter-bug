@page
@addTagHelper *, WhiteSpaceWriter-bug
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@using Microsoft.AspNetCore.Html
@{ 
    IHtmlContent GenerateContent(Func<int, IHtmlContent> handler)
    {
        var result = new HtmlContentBuilder();
        result.AppendLine(handler(42));
        return result;
    }
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Demo</title>
</head>
<body>
    <h1>There should be nothing but this heading</h1>
    <p remove-if-empty="true">
        @(GenerateContent(@<a asp-page="/index" asp-route-anwer="@item">Try again.</a>))
    </p>
</body>
</html>
